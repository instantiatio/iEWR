# S — exact source contribution, B1

Owner: `Binding`, exact identity observation. Public `inspect_binding(reader,
binding)` returns unmodified bytes or changed/missing binding. SourceReader is
an outward read-only port. No durable S record required for this exact use;
request/method refs are retained in X's attempted basis when execution begins.

Agent instruction: select only the current request and adequate supplied Method
description; inspect their exact bindings when needed for this use. Do not load
FPF, register a source or use a DPF namespace as a runtime route. Inspection does
not prove applicability, Method qualification, authority or conformance.

Allowed functional imports: none. S cannot call X/G/I or write an index in B1.
